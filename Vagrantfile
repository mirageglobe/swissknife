# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  #config.vm.box = "base"

  # using generic debian box - note newer version have issues on ssh with password auth
  config.vm.box = "generic/debian12"
  config.vm.box_version = "4.3.12"

  # config.vm.box_check_update = false
end
